/**
 * Small, runnable demonstrations of the public Blue Language API.
 *
 * <p>Every example has a {@code main} method for command-line use and a
 * deterministic {@code run} method used by the automated example tests. The
 * package demonstrates application-facing APIs; it does not define runtime
 * extensions or new Language semantics.</p>
 */
package blue.language.examples;
