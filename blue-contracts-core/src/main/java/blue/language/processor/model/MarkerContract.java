package blue.language.processor.model;

/**
 * Base contract representing declarative policy or state within a scope.
 */
public abstract class MarkerContract extends Contract {

    /** Creates an uninitialized marker contract. */
    public MarkerContract() {
    }
}
