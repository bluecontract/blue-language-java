/**
 * Immutable evidence boundary for affected-closure Contracts processing.
 *
 * <p>Every managed document is processed as its own Root. Relationship and
 * component evidence coordinates atomic closure scheduling without exposing a
 * containing-document context to an individual document invocation.</p>
 */
package blue.language.processor.closure;
