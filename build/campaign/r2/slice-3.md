# Slice 3: complete declaration boundary and independent assurance

Depends on slices 1 and 2. Its exact identity is the commit containing this report; final HANDOFF.md records the resolved hash.

Changes:

- Focused runtime and low-level specialization validate a definition-compatible overlay; the public runtime preprocesses the assembled source and returns an independent authored node. Payload-free constrained subtypes are authorable; invalid fixed prefixes still fail.
- Legacy Language canonicalization, minimization and subtype checks use definition preparation. ConformanceEngine's inline ancestry resolution does the same. Completed invocation resolution remains strict.
- All lone numeric bound keywords are validated before absent-payload predicate deferral. Frozen and mutable matching share schema semantics without exporting helper APIs.
- Limited resolution retains provider evidence inside one operation, including detached enum probes. The 96-entry counterexample drops from 9,217 provider reads to one under a one-ID budget.
- Simplified schema output preserves explicit large Integer keyword and custom enum types. The reproducer previously emitted source that could not preserve/reparse its constraints.
- Independent finite enum and binary64 numeric oracle, cross-representation partial-payload matrix, exact stronger-bound test and portable public SDK fixtures are included.
- The simple-schema serializer fixture now uses a legal unlabeled keyword value; it previously relied on discarding an invalid keyword label. Existing metadata-proof test fixture follows the explicit itemType proof goal; test style markers are corrected. No broad expected identity/gas output was recaptured.

Reports:

- declaration-contract.md: entrypoint certificates, partial-payload table, normative clauses, compatibility and structured diagnostics.
- oracle-and-counterexamples.md: seed, independent model, minimized failures and finite locality cases.
- sdk-handoff.md plus core test resources: reusable A2/C2 inputs and public SDK flow.
- fixture-definition-goal.patch: reviewable Language fixture migration for A2 to apply with authoritative regeneration. Released packaged fixture bytes are unchanged in this slice.

Full final validation and four expected authoritative binding/vector failures are recorded in verification.md and the final results JSON files. No HTTP, Contracts execution, Coordination, BEX, release sealing or regeneration claim is made.
