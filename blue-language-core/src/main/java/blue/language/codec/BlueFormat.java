package blue.language.codec;

/** Text formats accepted and emitted by the Blue codec. */
public enum BlueFormat {
    /** JavaScript Object Notation. */
    JSON,
    /** YAML restricted to the Blue JSON data model. */
    YAML
}
