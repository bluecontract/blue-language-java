# Retained replay identity cost

The MyOS composite negative-program document executes54 public operations and then restarts its56-command ledger. All rejection, state, event and gas checks passed on JAR01824b07, but retained replay took88.098seconds and failed the unchanged60-second restart bound.

A copied-database JFR reproduction showed repeated Base58 validation and semantic type-key accounting. The repair keeps the positive grammar cache at4096 slots and compares complete strings across at most16 slots, so colliding valid catalog IDs no longer continually evict one another. Invalid values never populate it. Plain IDs without a cyclic separator use the unchanged plain validator directly; cyclic syntax and exact error messages stay unchanged.

Semantic type keys still copy and compare the complete semantic graph. Their exact conservative retained-weight traversal now runs when accounting requests it and memoizes the result on that immutable key. Cache admission, oversized rejection and eviction still obtain the same complete weight before accepting retained data. There is no mutable-node, provider-result, authorization or gas cache.

The temporary two-class proposal passed189 focused/adjacent tests,48016 original-versus-proposed exact grammar/error comparisons and800 concurrent immutable-weight comparisons. A private copy of the same failed test database replayed all56 commands with exact replay verification in43.143seconds, with the same384MiB heap. This is diagnostic evidence, not final packaged acceptance. The normal component tests, coherent artifacts, complete public composite test and final gates remain required. Earlier preprocessing-configuration and alias-map cache experiments were not applied.
