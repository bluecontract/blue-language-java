# Active stabilization priority: packaged Java MyOS

The user's 2026-09-05 instruction in attachment `48acf1cc-f313-4de3-9628-1538adc43695/pasted-text.txt` replaces the order of work in the historical progress archive's STATUS.md.

1. Verify retained packages-03 generator archives and installed consumer bytes. Reproduce both retained catalog bundles twice from the committed nine-package authored input and explicit version-history bundles; require strict no-diff checks. Verify the catalog's exact/cyclic definitions and required workflow declarations with current immutable Java artifacts.
2. Freeze this generation boundary if those checks pass. The later partially verified JavaScript SDK/storage changes are preserved on `codex/stabilization-js-sdk-preserved` at `d0acfa39`; they are not a MyOS milestone gate. No general JS cleanup unless a concrete generator/Java-consumer failure requires it.
3. Checkpoint Language/Contracts and BEX; update necessary runtime identity, additive API, and profile bindings; stage a coherent immutable Language/BEX/catalog tuple. Keep full release certification separate from development staging.
4. Compile and test Coordination against those exact artifacts. Fix the first demonstrated owning-layer failure; do not run current downstream source against stale bootstrap artifacts.
5. Package MyOS and run focused public acceptance: empty/equivalent types, Gender/Foo, required request, child factory, embedded/shared child, late-content resume, restart. Then expand through original tutorials, cycles, gas and history.

The next substantive report must include an actual packaged MyOS test result or a precise Java/runtime blocker that prevents it. No stable/readiness claim until the declared acceptance surface passes.

Preservation: the earlier 58 MB progress archive remains immutable. Current source checkpoints and new evidence under `build/stabilization/java-priority` are later work, not bytes from that archive.
