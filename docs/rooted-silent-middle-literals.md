# Silent-middle production obligations

RCP-RUN-009 and RCP-RUN-010 have complete nine-step literal SDK plans. Their
original recipes are retained byte-for-byte under provenance/recipe-inputs,
with exact hashes. Both start S, M containing saved S0, and P containing saved M0.
A tick is submitted to S and processed from P. The independently authoritative S
and M histories stay unchanged while P's selected S, M and P identities advance.
009 has no intermediate or ancestor listener. 010 listens at the exact
/child/child path and receives one source event through the silent M.

The adapter records real implementation work and isolated step evidence, its
exact invocation binding, and before/after selected values. The checker permits
only containing-reference work at M and requires all three exact selected
identities and documents to change. It binds the calculated source event to the
full managed transition receipt, its exact predecessor/result, ordinal and bytes.

The first runtime attempt completed all steps but its extractor looked at public
result events for a root-local source. Those events correctly exclude the
unowned source. The extractor now reads the source's actual managed transition
receipt. It does not manufacture a public event or alter publication. A later
independent S operation must emit exactly one public source event, with P still
unchanged and quiescent. Retained-store restart compares complete records.

Both literals passed on MyOS JAR
3b9422207ecb6008c3c3c6e33cb12574bd41ff6b457d3a3ebe52a9aa7ec85b44.
Evidence: rooted-production-silent-middle-receipted-events.json. Eleven new
synthetic checker tests reject incomplete/wrong-invocation work, hidden middle
handler work, unchanged references, wrong lineages, duplicate/wrong source
events and unbound/wrong-position/altered source receipts. Synthetic tests
provide no runtime conformance credit. No production runtime or tariff changed.
