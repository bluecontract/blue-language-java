# Lists and incremental BlueId calculation

See [Lists and incremental identity](../guides/lists-and-incremental-identity.md)
for the recursive prefix fold, append complexity, suffix recomputation, and the
distinction between identity and storage.
