# RUN016 literal event-only oracle

The original recipe is preserved byte-for-byte with its SHA256 under provenance/recipe-inputs. The concrete plan starts independent S and empty P, saves S's initialized ID, then executes two actual event-only source operations100/150. Each emits two equal payload occurrences while preserving the processor state epoch. The host's actual receipt-backed revision2 is explicitly captured. After a store restart, P attaches the saved S0 at200 and traverses actual receipts1/2, with another restart between them and final no-work/restart checks.

The unchanged HISTORY_MULTIPLE verifier binds each occurrence, predecessor/successor, source order, exact source receipt, application receipt, original cause, companion and full metered trace. Literal independent anchors require source counter0, consumer log[0,0,0,0], source-only publication for each source operation, two distinct event ordinals per operation, unchanged processor epochs0/1, exactly two retained applications and preserved source evidence. Epoch2 is actual host history, never a synthetic processor revision. The real SDK counterexample previously failed before catch-up; the host/proof repair now passes the two SDK regressions and thirty adjacent tests.

Qualification means the recipe now has a complete executable plan and oracle. Production status remains NOT_RUN until the packaged-artifact runner actually executes it. Public MyOS tests additionally cover future500 beyond attachment200, durable worker ownership and fresh-JVM replay. No existing checker, oracle, tariff or original recipe is weakened.
