# Exact attachment positions and lagging-observer readiness

RUN033 now has four literal variants with identical real source operations at100 and150. Each starts an empty parent and attaches at200 using its saved authored original, initialized zero, retained one, or current two. Required source suffixes are respectively[0,1,2], [1,2], [2], and[]. The independent checker binds the literal capture to the actual exact Timeline Entry request, retains the captured initialization receipt, verifies each actual application/cause/receipt/companion and metered trace, requires final child2 and the appropriate event suffix, and compares the complete source records before/after. Every variant includes store reconstruction and no-work verification.

Contracts §2.3 permits the authored -1 to initialized0 step. Coordination ManagedEpochReceipt explicitly requires INITIALIZATION epoch0 with no previous managed BlueId. Its associated Contracts transition still binds the authored predecessor. The checker now verifies these two distinct facts using the explicitly supplied captured authored reference. Without that reference, an epoch-zero application rejects. Positive-epoch predecessor equality is unchanged. Eight focused initialization-oracle tests cover missing/wrong authored reference, fabricated prior managed state, wrong receipt kind, invented external source entry and changed transition endpoints. No receipt, cause format, runtime epoch or specification byte was changed.

RUN034 starts S and P->S0, retains P's ready view, then advances S independently through100 and150. Complete P bytes, epoch, history and advertised ready frontier remain unchanged, while its actual driver still selects100. After reconstruction P processes100 and then150; its advertised frontier advances only to the corresponding exact source order. Source state/history remain unchanged by observer processing. Nine focused checker tests reject false progress, wrong source-entry/document binding, rewritten old views/history and skipped pending inputs. This does not reinterpret READY as global queue emptiness.

Both original recipe files and their hashes are preserved under provenance/recipe-inputs. The new literals retain artifact-agnostic NOT_RUN metadata; production results are separate. On development JAR526715082665b44f44f031d702f2c8fbde0390dab74c7cf6a9cfe94692d1b25e both obligations passed, and sixteen independent mutations of the actual records rejected. The MyOS test-only verify_observation_negatives.py command reproduces those rejections without editing original evidence. Full final RC gates remain required.
