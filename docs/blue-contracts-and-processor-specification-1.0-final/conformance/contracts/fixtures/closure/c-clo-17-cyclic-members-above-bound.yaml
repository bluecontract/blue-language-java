schema: blue-contracts-closure-fixture/1.0
id: c-clo-17-cyclic-members-above-bound
vectors:
- C-CLO-17
category: limits
operation: limit-micro
description: The cyclicMembersPerComponent guard independently measures 129 against the frozen bound 128 and returns REJECT; this microfixture does not claim that a later gas-expensive invocation phase completes.
releaseManifest: ../../release-manifest.yaml
input:
  limit: cyclicMembersPerComponent
  configured: 128
  observed: 129
  generator:
    kind: cyclic-members
    parameters:
      count: 129
expected:
  limitDecision: REJECT
  rejectedStepAdmitted: false
  subsequentOutcome: NOT_EXECUTED_BY_MICROFIXTURE
  diagnostic: CyclicComponentMemberLimitExceeded
  rejectedStep: cyclicMembersPerComponent:129
