/**
 * Executes and verifies the closed Blue Contracts 1.0 closure corpus.
 *
 * <p>This package owns closure-fixture parsing, exact result comparison,
 * limit-micro evaluation, and the public-facade corpus harness. Runtime and
 * fixture lifecycle remain owned by the parent conformance package so that
 * production dependencies flow in one direction.</p>
 */
package blue.language.conformance.contracts.closure;
