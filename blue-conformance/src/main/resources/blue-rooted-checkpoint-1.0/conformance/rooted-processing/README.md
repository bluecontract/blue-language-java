# Rooted-processing fixtures — iteration 2

Start with [the index](FIXTURE_INDEX.md) and [the adapter contract](HARNESS.md). The 66 original abstract records and their independent model are preserved. Nine critical production cases now have literal sources/actions and strengthened exact outcome checks; the remaining 25 retain their recipe-level status. All 34 remain **NOT_RUN against a revised production runtime**.

`identity/` contains closed wrapper constructors and 13 unit vectors. `model/operation_model.py` is a finite ownership/cause model with illustrative gas, not Blue execution. The new negative tests reject the earlier all-error, empty-trace, incomplete-variant and wrong-artifact false positives.

The unchanged legacy corpus is under the package baseline. Its old PASS evidence cannot be imported as a result for this new profile. `requirement-coverage.json` states the scope of each evidence link and does not equate mapping with completeness.
