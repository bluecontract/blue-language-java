package blue.language.mapping;

/** Mapping-facing facade for the core annotated-type BlueId resolver. */
public class BlueIdResolver extends blue.language.utils.BlueIdResolver {

    /** Creates a facade over the shared deterministic resolver. */
    protected BlueIdResolver() {
    }
}
